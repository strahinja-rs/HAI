Build a tool that takes the book (epub, but also PDF if possible although PDF is probably harder), and then converts that book to multiple markdown files per chapters.

The output is a folder (can be zip or just a folder) of markdown chapters. This folder is placed directly into Inbox of HAI vault.